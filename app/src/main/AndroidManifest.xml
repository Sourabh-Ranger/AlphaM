<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alpha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Diet3Activity"></activity>
        <activity android:name=".Diet2Activity" />
        <activity android:name=".Diet1Activity" />
        <activity android:name=".DietActivity" />
        <activity android:name=".BmiActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".Music3Activity" />
        <activity android:name=".Music2Activity" />
        <activity android:name=".AlbumActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".MusicActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>